# ===========================================
# ENVIRONMENT CONFIGURATION
# ===========================================
# Copy this file to .env and update values

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
POSTGRES_DB=your_postgres_db
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5432

# ===========================================
# BACKEND CONFIGURATION
# ===========================================
BACKEND_PORT=8080
JWT_SECRET=your_jwt_secret_key
SUPERADMIN_DEFAULT_PASSWORD=1q2w#E$R

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
FRONTEND_PORT=3000

# ===========================================
# NGINX CONFIGURATION
# ===========================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ===========================================
# API & CORS CONFIGURATION
# ===========================================

# --- LOCAL DEVELOPMENT ---
# NEXT_PUBLIC_API_URL should NOT include /api path (nginx handles routing)
NEXT_PUBLIC_API_URL=http://localhost
ALLOWED_ORIGINS=http://localhost

# --- PRODUCTION WITH DOMAIN ---
# NEXT_PUBLIC_API_URL=https://seirotan.desa.id
# ALLOWED_ORIGINS=https://seirotan.desa.id

# ===========================================
# NOTES
# ===========================================
# - Nginx handles routing: / → frontend, /api → backend
# - Can access via domain (seirotan.desa.id) or IP (http://192.168.100.244)
# - Cloudflare Tunnel setup: seirotan.desa.id → http://localhost:80
# - No subdomain needed, everything via main domain with /api path
# - Database migrations run automatically on backend startup
# - Default superadmin user created with SUPERADMIN_DEFAULT_PASSWORD
